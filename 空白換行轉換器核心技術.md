# 空白換行轉換器：核心技術

## 一句話原理

這個工具不是改變 CSS 行距，而是把原本「完全空白的行」放入一個肉眼看不到的 Unicode 字元 `U+200B`，讓 Facebook、Instagram、Threads、WordPress 等平台把它視為有內容的行，避免連續換行被壓縮。

```text
轉換前：第一段 + 換行 + 空白行 + 換行 + 第二段
轉換後：第一段 + 換行 + U+200B + 換行 + 第二段
```

---

## 1. 空白換行轉換

### 原站實際程式邏輯

```js
const ZERO_WIDTH_SPACE = "\u200B";

function convertBlankLines(text) {
  return text.replace(
    /\n\n/g,
    `\n${ZERO_WIDTH_SPACE}\n`
  );
}
```

其中：

- `\n` 是換行字元 LF。
- `\n\n` 表示兩個連續換行，也就是中間存在一個空白行。
- `\u200B` 是零寬度空白字元 Zero Width Space。
- `g` 表示處理字串中所有符合的位置。

### 實際範例

輸入：

```text
今天要分享三個工作技巧。

第一個技巧是先整理任務。
```

程式看到的內容：

```js
"今天要分享三個工作技巧。\n\n第一個技巧是先整理任務。"
```

轉換後：

```js
"今天要分享三個工作技巧。\n\u200B\n第一個技巧是先整理任務。"
```

畫面看起來仍然是空白行，但中間那行其實含有一個 U+200B，因此平台較不容易將它刪除。

> 頁面文案寫「在換行處插入零寬度空白」，更精確的說法應是「在空白行插入零寬度空白」。單一換行不會被修改。

### 為什麼不能使用普通空格？

若空白行只放一般空格 `U+0020`，平台常會先執行 trim 或 whitespace normalization，空格仍可能被清除。U+200B 並不是一般的可見空格，顯示寬度是 0，但它在字串中仍是一個字元。

---

## 2. 更穩健的空白行轉換版本

原站使用 `/\n\n/g`，程式很短，但有幾個邊界情況：

- 沒有明確處理 Windows 的 CRLF：`\r\n`。
- 三個以上連續換行時，以「兩個一組」處理，結果不容易理解。
- 空白行若含空格或 Tab，就不符合 `\n\n`。
- 若文字已含其他零寬字元，沒有統一清理策略。

較穩健的實作方式是逐行處理：

```js
const ZERO_WIDTH_SPACE = "\u200B";

function convertBlankLines(text) {
  // 先把 Windows CRLF 與舊式 CR 統一成 LF。
  const normalized = text.replace(/\r\n?/g, "\n");

  // 每一個只有空白或 Tab 的行，都改成只含 U+200B 的行。
  return normalized
    .split("\n")
    .map(line => /^\s*$/.test(line) ? ZERO_WIDTH_SPACE : line)
    .join("\n");
}
```

如果不希望改動文字開頭或結尾的空行，可只處理位於兩段內容之間的空白行：

```js
const ZERO_WIDTH_SPACE = "\u200B";

function convertBlankLines(text) {
  const newline = text.includes("\r\n") ? "\r\n" : "\n";
  const lines = text.replace(/\r\n?/g, "\n").split("\n");

  const converted = lines.map((line, index) => {
    const isBlank = /^[\t ]*$/.test(line);
    const hasContentBefore = lines
      .slice(0, index)
      .some(item => item.trim() !== "");
    const hasContentAfter = lines
      .slice(index + 1)
      .some(item => item.trim() !== "");

    return isBlank && hasContentBefore && hasContentAfter
      ? ZERO_WIDTH_SPACE
      : line;
  });

  return converted.join(newline);
}
```

---

## 3. 中文與英文／數字排版優化

這部分和空白換行是兩個獨立功能。它會偵測：

- 中文等 CJK 字元後面緊接英文或數字。
- 英文或數字後面緊接 CJK 字元。
- CJK 與指定 ASCII 符號 `@#$%^&*` 緊貼。

找到交界後，插入一般半形空白 `U+0020`。

### 原站核心演算法

```js
const CJK = "⺀-⻿⼀-⿟぀-ゟ゠-ヿ㄀-ㄯ㈀-㋿㐀-䶿一-鿿豈-﫿︰-﹏";

function optimizeChineseSpacing(text) {
  let result = text;

  result = result.replace(
    new RegExp(`([${CJK}])([A-Za-z0-9])`, "g"),
    "$1 $2"
  );

  result = result.replace(
    new RegExp(`([A-Za-z0-9])([${CJK}])`, "g"),
    "$1 $2"
  );

  result = result.replace(
    new RegExp(`([${CJK}])([@#$%^&*])`, "g"),
    "$1 $2"
  );

  result = result.replace(
    new RegExp(`([@#$%^&*])([${CJK}])`, "g"),
    "$1 $2"
  );

  return result;
}
```

### `$1 $2` 是什麼？

正則中的兩組括號會保存相鄰的兩個字元：

```text
$1 = 左側字元
空格 = 新插入的半形空白
$2 = 右側字元
```

例如：

```text
原文：使用ChatGPT寫3篇Threads貼文
結果：使用 ChatGPT 寫 3 篇 Threads 貼文
```

這是「盤古之白」概念的精簡實作，但不是完整的 pangu.js。它沒有涵蓋所有標點、Unicode 拉丁字母與特殊組合。

---

## 4. 字數統計

原站直接使用：

```js
const count = text.length;
```

這會計算 JavaScript UTF-16 code unit 數量：

```js
"中文".length; // 2
"A".length;    // 1
"😀".length;   // 2，不是人眼認知的 1 個字元
```

若只需要簡單提示，`length` 足夠。若要正確計算使用者看到的字素，應使用 `Intl.Segmenter`：

```js
function countVisibleCharacters(text) {
  const segmenter = new Intl.Segmenter("zh-TW", {
    granularity: "grapheme"
  });

  return [...segmenter.segment(text)].length;
}
```

如果目標是檢查 Facebook、Instagram 或 Threads 的貼文字數上限，還要依各平台規則個別計算，不能假設它們都等同 JavaScript `length`。

---

## 5. 一鍵複製

轉換結果透過瀏覽器 Clipboard API 寫入剪貼簿：

```js
async function copyResult(result) {
  try {
    await navigator.clipboard.writeText(result);
    return { ok: true };
  } catch (error) {
    console.error("複製失敗：", error);
    return { ok: false, error };
  }
}
```

Clipboard API 通常需要：

- 網站在 HTTPS 或 localhost 執行。
- 由使用者點擊等操作觸發。
- 瀏覽器允許存取剪貼簿。

產品上仍應顯示一個可選取的輸出框，讓 Clipboard API 失敗時可以手動複製。

---

## 6. 四個核心功能如何串起來

```js
const input = document.querySelector("#input");
const convertButton = document.querySelector("#convert");
const optimizeButton = document.querySelector("#optimize");
const counter = document.querySelector("#counter");
const preview = document.querySelector("#preview");

input.addEventListener("input", () => {
  counter.textContent = countVisibleCharacters(input.value);
  preview.textContent = input.value;
});

convertButton.addEventListener("click", async () => {
  const result = convertBlankLines(input.value);
  preview.textContent = result;
  await copyResult(result);
});

optimizeButton.addEventListener("click", async () => {
  const result = optimizeChineseSpacing(input.value);
  preview.textContent = result;
  await copyResult(result);
});
```

完整資料流是：

```text
textarea 輸入
    │
    ├─ 即時更新字數
    ├─ 即時更新預覽
    │
    ├─ 空行轉換 → 插入 U+200B → 預覽 → 複製
    │
    └─ 中文排版 → CJK 邊界補 U+0020 → 預覽 → 複製
```

原站的兩個按鈕各自獨立執行。如果希望一次完成全部功能，可建立組合函式：

```js
function optimizeAll(text) {
  return convertBlankLines(
    optimizeChineseSpacing(text)
  );
}
```

---

## 7. 必測案例

```js
console.assert(
  convertBlankLines("甲\n\n乙") === "甲\n\u200B\n乙"
);

console.assert(
  optimizeChineseSpacing("中文ABC中文") === "中文 ABC 中文"
);

console.assert(
  optimizeChineseSpacing("第3篇") === "第 3 篇"
);

console.assert(
  convertBlankLines("沒有空行") === "沒有空行"
);
```

正式版本還應測試：

- `\r\n` Windows 換行。
- 三個以上連續換行。
- 空白行含空格或 Tab。
- 已經含有 U+200B 的文字。
- emoji、合成 emoji 與罕見 Unicode 字元。
- Clipboard 權限被拒絕。

---

## 結論

這個功能的核心不需要後端，也不需要 AI：

1. 使用正則或逐行處理找出空白行。
2. 在空白行加入 `U+200B` 零寬度空白。
3. 用正則在 CJK 與英數交界加入 `U+0020` 半形空白。
4. 用 JavaScript 計算字數並即時預覽。
5. 用 Clipboard API 將結果複製，供使用者貼到社群平台。

最關鍵的一行就是：

```js
text.replace(/\n\n/g, "\n\u200B\n");
```

